{
  "name": "@snz-rodoved/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/server.js",
  "types": "./dist/server.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.0",
    "@fastify/cors": "^10.0.0",
    "@fastify/static": "^9.1.3",
    "@snz-rodoved/db": "0.1.0",
    "@snz-rodoved/shared": "0.1.0",
    "dotenv": "^16.4.0",
    "fastify": "^5.0.0",
    "zod": "^3.23.0"
  }
}
